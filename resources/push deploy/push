git add .
git commit -m "v1.12.10 - Correcciones y Fix: Internet lento con timeout 30s + reintentos, delays 2s entre impresiones, fix enforceFocus loop infinito"

# Usa la variable GH_TOKEN global de Windows (sin exponerlo)
npm run deploy